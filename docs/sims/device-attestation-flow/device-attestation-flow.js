// CANVAS_HEIGHT: 660
// Device Attestation Flow — Mermaid sequenceDiagram.
// Bloom: Understand. A static, top-to-bottom remote-attestation exchange among
// the Verifier, the Device, and the TPM / hardware root of trust. The amber
// note over the Verifier holds the four checks; a styled HTML note below the
// diagram states the trust caveat (EK certificate + current PCR policy).
//
// Sequence diagrams have no per-node hover targets, so interactivity is the
// numbered, color-coded message flow plus the highlighted policy note rather
// than tooltips. This file exists mainly to carry the CANVAS_HEIGHT comment
// that fix-iframe-heights.py reads.
